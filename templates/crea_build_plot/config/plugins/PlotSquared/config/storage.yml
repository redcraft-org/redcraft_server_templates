prefix: 'ps_crea_build_plot_'

# SQLite section
sqlite:
  # Should SQLite be used?
  use: false
  # The file to use
  db: "storage"

# MySQL section
mysql:
  # Should MySQL be used?
  use: true
  host: "10.0.2.42"
  port: "31006"
  user: "minecraft"
  password: "MYSQL_PASSWORD"
  database: "minecraft"
  # Set additional properties: https://goo.gl/wngtN8
  properties:
  - "useSSL=false"
